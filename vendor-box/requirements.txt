# Pinned for CUDA 12.1 on the vendor box. DO NOT install on Mac dev (CUDA-only).
# Run: pip install -r requirements.txt

# Web service
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
python-multipart==0.0.20

# Image / video processing
pillow==11.0.0
opencv-python-headless==4.10.0.84
numpy==2.1.3

# Torch — CUDA 12.1 wheels. Pin to versions tested with the models below.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# Diffusers / transformers for IDM-VTON + Wan 2.1
diffusers==0.31.0
transformers==4.46.3
accelerate==1.2.1
safetensors==0.4.5
einops==0.8.0

# Tensor-RT runtime for DM-VTON live mode (~50ms/frame on RTX 4080).
# Install separately via NVIDIA's apt repo — do NOT pip-install (pip wheels are stale).
# See README setup section. The dm_vton_loader imports `tensorrt` from system.

# Body parsing for VTON pre-processing (segments person → cloth-only mask)
mediapipe==0.10.18

# HuggingFace hub for model downloads
huggingface_hub==0.27.0
